# API documentation

No public HTTP API is currently shipped. The importer uses the source MediaWiki
API internally, but that external endpoint is not exposed as a Nazca Railway
product API. A Postman collection is therefore not applicable.
